import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Encostay',
  appName: 'Encostay',
  webDir: 'build',
  server: {
    androidScheme: 'https',
  }
};

export default config;
