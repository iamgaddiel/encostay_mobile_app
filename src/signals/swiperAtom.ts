import { effect, signal } from "@preact/signals-react"
import Swiper from "swiper"


export const passwordResetSwipeSignal = signal<any>(Swiper)
export const onBoardingSwipeSignal = signal<any>(Swiper)

